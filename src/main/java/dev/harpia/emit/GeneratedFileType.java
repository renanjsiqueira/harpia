package dev.harpia.emit;

/** Stable artifact categories used by diagnostics, tests and future source-map tooling. */
public enum GeneratedFileType {
    JAVA_SOURCE,
    BUILD,
    CONFIGURATION,
    MIGRATION,
    OTHER
}
